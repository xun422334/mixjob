services:
  backend:
    build: .
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./browser_states:/app/browser_states
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
